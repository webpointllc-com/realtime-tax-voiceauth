/** Total visible S-PUL skeleton teaser per cycle (Proto C — not Proto B splash+dwell). */
export const SPUL_TEASER_MS = 2000;

/** Overlay fade in/out (matches Proto B gate fade). */
export const SPUL_GATE_FADE_MS = 320;

/** Time on Prototype C security UI before the teaser replays. */
export const SPUL_SECURITY_DWELL_MS = 4000;
